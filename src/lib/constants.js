/* Bump this on every update/push so the footer marker shows what's deployed.
   The Sep 2026 Index-first rebrand opens the v3 line. */
export const SITE_VERSION = "v3.0.0.0";

/* Every call to action on the site points at the Google Form. Change this one
   value to repoint all of them. */
export const REGISTER_URL = "https://forms.gle/mL4PZKgog7kbjdQC7";

/* The primary CTA, as specified in Ken's document. The form behind it collects
   interest and feedback rather than producing a score on the spot, so anywhere
   this label appears at full size it is paired with REGISTER_NOTE — the CTA
   should read as an invitation, not as a promise the product can't yet keep. */
export const REGISTER_LABEL = "Discover Your Index";
export const REGISTER_NOTE =
  "Register your interest and we'll tell you the moment your Index is ready.";

export const CONTACT_EMAIL = "hello@theultimatehuman.fitness";
